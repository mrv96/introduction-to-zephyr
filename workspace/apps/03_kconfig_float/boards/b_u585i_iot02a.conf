CONFIG_SERIAL=y # auto added saving minimal config
CONFIG_GPIO=y # auto added saving minimal config
CONFIG_CONSOLE=y # auto added saving minimal config
CONFIG_UART_CONSOLE=y # auto added saving minimal config
